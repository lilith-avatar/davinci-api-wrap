---@class Timer
local Timer = {}

---获取从游戏开始到当前时刻所经历的时间，以秒为单位。
---@return number
function Timer.GetTime()
end

---获取从游戏开始到当前时刻所经历的时间，以毫秒为单位。
---@return number
function Timer.GetTimeMillisecond()
end

return Timer